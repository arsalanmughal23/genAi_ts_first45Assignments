function makeSandwich(items:string[]){
    console.log(`Your sandwich is ready with ingredients: ${items.join(', ')}`);
}
makeSandwich(['chicken', 'jalapeno', 'peperoni']);
makeSandwich(['jalapeno', 'eggs']);
makeSandwich(['olives']);